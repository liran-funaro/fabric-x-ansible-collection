#
# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#

- name: Transfer TLS CA certificate of Fabric CA
  when:
    - hostvars[organization.fabric_ca_host].fabric_ca_use_tls is defined
    - hostvars[organization.fabric_ca_host].fabric_ca_use_tls
  block:
    - name: Ensure TLS CA certificate folder exists
      ansible.builtin.file:
        path: "{{ yugabyte_remote_config_dir }}/tlscacerts"
        state: directory
        mode: "0o755"
    - name: Copy TLS CA certificate of Fabric CA
      ansible.builtin.copy:
        src: "{{ fetched_artifacts_dir }}/crypto/peerOrganizations/{{ organization.domain }}/msp/tlscacerts/tlsca.{{ organization.domain }}-cert.pem"
        dest: "{{ yugabyte_remote_config_dir }}/tlscacerts"
        mode: "preserve"

- name: Initialize Yugabyte TLS SAN hosts
  ansible.builtin.set_fact:
    yugabyte_tls_san_hosts: "{{ ansible_all_ipv4_addresses + [ansible_host, actual_host] }}"

- name: Append every cluster node's address to Yugabyte TLS SAN hosts
  # A smart-driver client with load_balance=true discovers the other nodes through
  # yb_servers() and moves connections onto them, but it rewrites only the host, port and
  # fallback list on the connection config -- not TLSConfig.ServerName. Under
  # sslmode=verify-full the peer's certificate is then verified against the address of the
  # endpoint first dialled, and fails:
  #
  #   x509: certificate is valid for 10.241.64.14, not 10.241.64.10
  #
  # Every discovered node is marked unavailable and the client silently keeps all of its
  # connections on that first endpoint, capping the cluster at one node's SQL front end.
  # Naming all cluster addresses in each node's certificate makes verification succeed
  # whichever node the balancer lands on.
  #
  # The nodes of one cluster already share a CA and are mutually trusted peers, so this does
  # not widen the trust boundary beyond the cluster. It does let any node within it present
  # itself as any other, which is why it is opt-in.
  ansible.builtin.set_fact:
    yugabyte_tls_san_hosts: >-
      {{
        (yugabyte_tls_san_hosts
         + (yugabyte_cluster | map('extract', hostvars, 'ansible_host') | list))
        | unique
        | list
      }}
  when:
    - yugabyte_tls_san_all_cluster_nodes
    - yugabyte_cluster is defined

- name: Append master webserver route to Yugabyte TLS SAN hosts
  ansible.builtin.set_fact:
    yugabyte_tls_san_hosts: "{{ yugabyte_tls_san_hosts + [yugabyte_openshift_master_webserver_route] }}"
  when:
    - yugabyte_component_type == 'master'
    - yugabyte_openshift_master_webserver_route is defined
    - yugabyte_openshift_master_webserver_route | length > 0

- name: Append tablet webserver route to Yugabyte TLS SAN hosts
  ansible.builtin.set_fact:
    yugabyte_tls_san_hosts: "{{ yugabyte_tls_san_hosts + [yugabyte_openshift_tablet_webserver_route] }}"
  when:
    - yugabyte_component_type == 'tablet'
    - yugabyte_openshift_tablet_webserver_route is defined
    - yugabyte_openshift_tablet_webserver_route | length > 0

- name: Append tablet pgsql web route to Yugabyte TLS SAN hosts
  ansible.builtin.set_fact:
    yugabyte_tls_san_hosts: "{{ yugabyte_tls_san_hosts + [yugabyte_openshift_tablet_pgsql_web_route] }}"
  when:
    - yugabyte_component_type == 'tablet'
    - yugabyte_openshift_tablet_pgsql_web_route is defined
    - yugabyte_openshift_tablet_pgsql_web_route | length > 0

- name: Append tablet cql web route to Yugabyte TLS SAN hosts
  ansible.builtin.set_fact:
    yugabyte_tls_san_hosts: "{{ yugabyte_tls_san_hosts + [yugabyte_openshift_tablet_cql_web_route] }}"
  when:
    - yugabyte_component_type == 'tablet'
    - yugabyte_openshift_tablet_cql_web_route is defined
    - yugabyte_openshift_tablet_cql_web_route | length > 0

- name: Generate Yugabyte TLS crypto material
  vars:
    fabric_ca_host: "{{ organization.fabric_ca_host }}"
    fabric_ca_use_tls: "{{ hostvars[organization.fabric_ca_host].fabric_ca_use_tls | default(false) }}"
    fabric_ca_name: "{{ hostvars[organization.fabric_ca_host].fabric_ca_name }}"
    fabric_ca_msp_dir: "{{ yugabyte_remote_config_dir }}/tls"
    fabric_ca_tls_certfile: "{{ yugabyte_remote_config_dir }}/tlscacerts/tlsca.{{ organization.domain }}-cert.pem"
    fabric_ca_enrollment_profile: tls
    fabric_ca_cryptogenize_tls_cert_file: node.{{ ansible_host }}.crt
    fabric_ca_cryptogenize_tls_key_file: node.{{ ansible_host }}.key
    fabric_ca_csr_hosts: "{{ yugabyte_tls_san_hosts }}"
    fabric_ca_identity:
      name: "{{ organization.peer.name }}"
      secret: "{{ organization.peer.secret }}"
  ansible.builtin.include_role:
    name: hyperledger.fabricx.fabric_ca
    tasks_from: client/enroll
